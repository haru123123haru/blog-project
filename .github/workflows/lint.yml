name: HTML Linter

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run HTML Linter
        uses: anishathalye/html-linter@v1
        with:
          fail-on-error: true